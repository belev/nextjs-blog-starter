---
title: 'Supported elements'
date: '2020-05-16'
description: 'Example of code highlighting and different elements in a blog post.'
---

![Code thinking](./code-thinking.png)

## Example of different elements from blog post

### Unorderd list

- first item
- second item with `code example`
- third item

### Ordered list

1. First
2. Second
3. Third

### Link

Build with [Next.js](https://nextjs.org/).

### Quote

> This is a quote

### Code highlighting

#### Basic

```tsx
import { useRouter } from 'next/router';
import classNames from 'clsx';
import Link from '../Link';
import styles from './NavigationLink.module.scss';

type Props = React.PropsWithChildren<{ href: string }>;

const NavigationLink = ({ href, children }: Props) => {
  const router = useRouter();
  const isActive = router.asPath === href;

  return (
    <Link
      href={href}
      className={classNames(styles.navigationLink, {
        [styles.active]: isActive
      })}
    >
      {children}
    </Link>
  );
};
```

#### With line numbers

```tsx showLineNumbers
import { useRouter } from 'next/router';
import classNames from 'clsx';
import Link from '../Link';
import styles from './NavigationLink.module.scss';

type Props = React.PropsWithChildren<{ href: string }>;

const NavigationLink = ({ href, children }: Props) => {
  const router = useRouter();
  const isActive = router.asPath === href;

  return (
    <Link
      href={href}
      className={classNames(styles.navigationLink, {
        [styles.active]: isActive
      })}
    >
      {children}
    </Link>
  );
};
```

#### With highlighted lines

```tsx {12-21}
import { useRouter } from 'next/router';
import classNames from 'clsx';
import Link from '../Link';
import styles from './NavigationLink.module.scss';

type Props = React.PropsWithChildren<{ href: string }>;

const NavigationLink = ({ href, children }: Props) => {
  const router = useRouter();
  const isActive = router.asPath === href;

  return (
    <Link
      href={href}
      className={classNames(styles.navigationLink, {
        [styles.active]: isActive
      })}
    >
      {children}
    </Link>
  );
};
```

#### With highlighted lines and line numbers

```tsx {12-21} showLineNumbers
import { useRouter } from 'next/router';
import classNames from 'clsx';
import Link from '../Link';
import styles from './NavigationLink.module.scss';

type Props = React.PropsWithChildren<{ href: string }>;

const NavigationLink = ({ href, children }: Props) => {
  const router = useRouter();
  const isActive = router.asPath === href;

  return (
    <Link
      href={href}
      className={classNames(styles.navigationLink, {
        [styles.active]: isActive
      })}
    >
      {children}
    </Link>
  );
};
```

### To do

#### HR

---
